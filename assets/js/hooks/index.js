// Every hook the app defines, keyed by the name used in `phx-hook`.
import AnchoredPanel from "./anchored_panel"
import DeferToAction from "./defer_to_action"
import SidebarToggle from "./sidebar_toggle"

export default {AnchoredPanel, DeferToAction, SidebarToggle}
